import { TaskGlobe as TaskGlobeComponent } from '@/components/TaskGlobe';

const TaskGlobePage = () => {
  return (
    <div className="min-h-screen w-full">
      <TaskGlobeComponent />
    </div>
  );
};

export default TaskGlobePage;
